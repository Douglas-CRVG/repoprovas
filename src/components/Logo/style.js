import styled from "styled-components";

const SCLogo = styled.div`
  display: flex;
  align-items: center;
  gap: 0 12.25px;
  width: max-content;

  img {
    width: 61.74px;
    height: 63.5px;
  }

  p {
    font-family: "Lexend", sans-serif;
    font-weight: 700;
    font-size: 36px;
    line-height: 45px;
  }

  strong {
    color: #3f61d7;
  }
`;

export { SCLogo };
